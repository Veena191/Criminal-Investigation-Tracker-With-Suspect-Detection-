# Criminal-Investigation-Tracker-With-Suspect-Detection-
Criminal Investigation Tracker is a Flask-based API that manages criminal reports using a CSV-backed database. It uses a Random Forest ML model to compare new cases with existing records and detect similarities. The system provides GET, POST, and DELETE endpoints to efficiently manage investigation data.
